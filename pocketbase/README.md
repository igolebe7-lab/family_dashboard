# PocketBase

PocketBase backend lives here.

Expected local run after installing the PocketBase binary manually:

```bash
cd pocketbase
./pocketbase serve --http=127.0.0.1:8090
```

Do not commit the PocketBase binary or `pb_data`.

Planned structure:

```txt
pb_hooks/
pb_migrations/
pb_public/
```
