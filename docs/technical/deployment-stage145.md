# Stage 14.5: размещение и тестовая семья

Дата: 2026-09-14. Владелец подтвердил ручной backup и разрешил размещение.

## Размещение

- URL: https://147.45.136.245/login
- Активный релиз: `/opt/familytime/releases/familytime-20260915T130633`.
- Caddy 2.11.4: проверенный официальный Linux binary, отдельная служба.
- PocketBase 0.38.2 с Go Web Push и существующими JS hooks; слушает только 127.0.0.1:8090.
- Certbot 5.8.0 в `/opt/familytime-certbot`, без изменения системного Python или общего apt upgrade.
- Сертификат Let's Encrypt с IP SAN; текущий notAfter: 2026-09-21 04:37:29 UTC.
- `familytime-certbot.timer`: 00:15, 08:15, 16:15 серверного времени плюс случайная задержка до 30 минут; persistent.
- `familytime-backup.timer`: ежедневно; сохраняет последние 7 архивов. SQLite архивируется при краткой остановке только PocketBase.
- PocketBase rate limits включены. Клиентский IP берётся из X-Forwarded-For, заданного Caddy; backend не открыт наружу.
- Администратор не доступен через публичный proxy; данные доступа хранятся root-only, не в git.

## Тестовая семья

«Тестовая семья», Europe/Moscow: Игорь (owner), Катя (parent), Ева (child).
У каждого свой тестовый аккаунт; у Евы managed_by включает обоих родителей.
Катя и Ева связаны с профилями через обычный invite/accept flow.

Созданы два информационных повторяющихся события (работа по будням и рисование
по понедельникам/средам), поручение «Собрать рюкзак», задача на завтра, private
запись Игоря и событие adults. Дополнительное поручение оставлено подтверждённым
как пример полного flow. Повторы тестового расписания ограничены двумя неделями.

Тестовые адреса не являются почтовыми ящиками. Пароли и локальные резервные копии
находятся в `.local/`, исключённом из git; права на доступы 0600, каталог 0700.
Новые аккаунты не привязываются к тестовой семье автоматически.

## Проверки

- HTTPS проверен с внешнего компьютера без отключения проверки сертификата.
- Staging issuance и `certbot renew --dry-run` прошли. Deploy hook проверяет IP и срок сертификата перед копированием и reload Caddy.
- API health, регистрация, принятие приглашений, отсутствие семьи у нового пользователя до invite, private/adults visibility прошли.
- Chrome через Playwright: desktop 1440x900 и mobile 390x844, login, Today, Calendar, Family, настройки уведомлений, реальный service worker. Нет console/page errors или горизонтального переполнения. Browser plugin отсутствовал.
- Две отдельные сессии: child done, parent inbox обновился через realtime без reload, parent approve, child notification и item_activity появились.
- Backup восстановлен в отдельный каталог: SQLite integrity_check=ok, 1 семья/3 пользователя; восстановленный PocketBase запущен на временном loopback-порту, health=200. Временная копия и процесс удалены; live DB не заменялась.
- Проверенная копия перенесена на компьютер; SHA256 совпадает. Это backup приложения, не замена snapshot всего VPS.
- Amnezia AWG не перезапускалась (контейнер запущен 2026-07-10, restart count=0), при проверке передавала трафик. У обычного WG и AWG свежие handshake. Docker, UFW/iptables, маршруты, VPN config и reboot не изменялись.
- После smoke: около 421 МБ available RAM; PocketBase/Caddy вместе около 53 МБ по MemoryCurrent, NRestarts=0. Это короткий замер, не нагрузочная гарантия.
- `pnpm check`, `pnpm lint`, `pnpm test` (223), `pnpm build`, ShellCheck пройдены.

## Исправления, найденные при размещении

1. macOS metadata в tar превращались на Linux в `._*.js`, ломая запуск миграций.
   Упаковщик отключает xattrs/AppleDouble; исправленный архив проверен на Linux.
2. ISO timestamps с `T` и offset в PocketBase filter сравнивались со строковыми
   UTC датами SQLite некорректно: выпадал первый день, нарушались границы сводки.
   Добавлена единая нормализация дат для range, attention, schedule и read snapshot.
   Регрессионные тесты и повторный live UI smoke подтвердили сегодняшние записи.

## Мобильное исправление после размещения

- Убран отрицательный нижний margin у Today, который сокращал доступную прокрутку
  на 124 px. Добавлен нижний отступ с учётом фиксированной навигации и safe area.
- На мобильных и сенсорных устройствах текстовые поля, textarea и select имеют
  шрифт не менее 16 px для предотвращения автоматического приближения при фокусе.
  Ручной pinch zoom не отключён; принудительного сброса масштаба нет.
- До исправления последняя кнопка находилась на 80 px ниже верхней границы меню
  даже при максимальной прокрутке. После исправления между ними около 40 px.
- Playwright: Chrome и WebKit, размеры 375x667, 390x844, 430x932; доступны быстрые
  действия, поля login/composer не мельче 16 px, закрытие модалки восстанавливает
  прокрутку. Проверки повторены на публичном HTTPS для Chrome и WebKit.
- Перед обновлением создан `/var/backups/familytime/familytime-20260914T142912Z.tar.gz`.
  Предыдущий релиз сохранён; backend binary и миграции совпадают. WG активен,
  Amnezia AWG продолжает работать с прежним StartedAt и restart count=0.
- Для уже открытой PWA нужно применить обновление кнопкой «Обновить».
  Поведение экранной клавиатуры и масштаба на физическом iPhone ещё требует проверки.

## Stage 14.6: мобильный календарь

2026-09-14: размещён frontend-релиз `familytime-20260914T205058`.
Backup перед обновлением: `/var/backups/familytime/familytime-20260914T205139Z.tar.gz`.
Backend binary и миграции совпадают с предыдущим релизом; VPN не перезапускался.

- На телефоне доступны День/Неделя/Месяц с явным выделением активного режима.
- Неделя использует общую с desktop временную сетку и прокрутку по двум осям;
  заголовки дней и шкала времени закреплены. Дни расширяются под читаемые
  параллельные колонки событий. Отдельно учтены пересечения коротких карточек
  по минимальной визуальной высоте, а не только по фактической длительности.
- В режиме дня снято ограничение высоты вложенного списка, строки больше не
  сжимаются; линия собирается из сегментов строк и доходит до последней точки.
- Обрабатываются rejected promises от async unsubscribe PocketBase при быстрых
  переходах. Локальные listeners SDK удаляет до сетевого подтверждения отписки.
- Check/lint: 0 ошибок/предупреждений, test: 229, production build пройден.
  Playwright Chrome/WebKit: 320/390 px, desktop 1440 px; режимы, навигация назад,
  модалки, обе оси прокрутки, закрепление координат, отсутствие пересечений
  карточек. Публичная HTTPS-версия проверена повторно после обновления.
- Реальные touch-жесты Safari и системные push на физическом iPhone ещё требуют
  ручной проверки; автоматизированный WebKit не заменяет устройство.

## Stage 14.6: мобильные темы

Опубликован релиз `familytime-20260914T212301`: Pearl Garden / Twilight Crystal,
настройка светлой, тёмной и системной темы в профиле. Предпочтение сохраняется
на устройстве и применяется до гидратации. Новых зависимостей и серверных
служб нет; бинарник backend и миграции побайтово совпадают с предыдущим релизом.

Backup перед обновлением: `/var/backups/familytime/familytime-20260914T212546Z.tar.gz`.
Предыдущий релиз `familytime-20260914T205058` сохранён для отката.
Публичная HTTPS-версия проверена в WebKit: обе темы, системное переключение,
reload, Today/Calendar/Assignments/Family/Profile, модалка и desktop.
Локальный static smoke подтвердил прокрутку недели и доступность быстрых действий.
Обе VPN-службы активны; `amnezia-awg2` сохраняет прежний StartedAt и restarts=0.

## Stage 14.6.2–14.6.5: публикация общего мобильного оформления

2026-09-15: опубликован `familytime-20260915T111144` с разрешения владельца.
Общий glass-стиль основных мобильных экранов, обе темы, карточки семьи,
сводка дня и модалки; исправлены focus trap и прокрутка формы в WebKit.
Backup: `/var/backups/familytime/familytime-20260915T111438Z.tar.gz`.
Предыдущий релиз `familytime-20260914T212301` сохранён для отката.
Backend binary и миграции совпадают с предыдущим релизом.
Check/lint/build пройдены, 241 unit-тест и Go-тесты прошли.
Публичный HTTPS smoke WebKit: 10 разделов, обе темы, 320/390 px и desktop,
модалки, клавиатурный фокус, редактирование без сохранения и быстрые действия.
Service worker в этом UI smoke отключён; это не проверка обновления PWA на iPhone.
PocketBase, Caddy и wg0 активны; AWG не перезапущен, StartedAt неизменен,
restarts=0. Сеть, firewall, Docker и настройки VPN не изменялись.

## Оставшиеся проверки после публикации

### Stage 14.6.8 / 14.7: диагностика PWA и push, 2026-09-15

- Реальное обновление в Chrome между сборками 212301 и 111144: waiting worker,
  подтверждение, активация и reload прошли. Сессия, тема и sessionStorage сохранены;
  offline shell и восстановление подключения проверены. Это не физический iPhone.
- На сервере найдены две неуспешные отправки Apple: HTTP 403. Regression test
  обнаружил `mailto:mailto:` в VAPID sub: webpush-go v1.4 самостоятельно добавляет
  префикс. Контакт исправлен на границе вызова библиотеки; тест сначала падал,
  затем прошёл. Ключи и подписки не заменялись.
- Исправление опубликовано в 125844; backup 20260915T125956Z, предыдущий 111144
  сохранён. Схема неизменна, изменён backend binary. VPN не перезапускались.
- Check/lint/build, 241 frontend-тест и Go-тесты прошли. Фактическую доставку
  на устройства нельзя считать подтверждённой только по постановке в очередь.
- Desktop Chrome проверен отдельным постоянным профилем: настоящая подписка,
  отправка через provider, появление уведомления в `getNotifications()` service
  worker, отключение подписки прошли. Тестовая подписка удалена через UI.
  Приватный контекст Chrome отвергал регистрацию с `permission denied`.
  Наличие системного баннера при пользовательском Focus не подтверждается
  одним лишь `getNotifications()`. Владелец затем подтвердил получение push на iPhone.
- 130633 добавляет русские сообщения отказа браузера/сети вместо raw failed.
  Backup: `/var/backups/familytime/familytime-20260915T130808Z.tar.gz`.
  Повторные check/lint/build и 242 frontend-теста прошли.

### Stage 14.7.1–14.7.5: обычный профиль Chrome

2026-09-15: в обычном Chrome на Mac выполнен вход тестовой Катей. После
браузерного запроса и выбора «Разрешить» подписка включилась. В macOS найдены
две записи Google Chrome с выключенными уведомлениями; обе включены, настройки
других приложений и общий режим показа при трансляции экрана не изменялись.

Вкладка FamilyTime закрыта, Chrome оставлен запущенным. Для одной временной
private-задачи серверный cron создал одно напоминание в 13:58 UTC, provider
ответил 201, попытка одна. После последующих проходов cron дублей нет.
Это подтверждает отправку без вкладки, но не заменяет проверку системного баннера.
Первую задачу архивировали; повторная проверка после настройки macOS выполняется
на отдельной временной записи. iPhone-подписка Игоря не отключалась.

Повторное напоминание `h7ejsynk6rmqffe` отправлено cron в 14:04 UTC: 201,
одна попытка; после нескольких минут по-прежнему одна notification. Запись пока
оставлена для ручного клика из системного уведомления, затем её нужно архивировать.
До включения системного разрешения владелец сообщил, что уведомления в центре
macOS нет; после включения подтверждение видимого баннера и клика пока не получено.
Сам центр уведомлений недоступен CUA (timeout), поэтому Stage 14.7.4 не закрыт.

Stage 14.7.5: отдельный постоянный профиль Chrome тестовой Кати прошёл реальную
регистрацию и logout. После выхода PushSubscription отсутствует, device в
IndexedDB удалён, getNotifications пуст; запрос тестовой отправки со старым
device secret и ещё действительным auth token получает 403. На сервере остались
только две исходные подписки: iPhone Игоря и обычный Chrome Кати. Обычный Chrome
оставлен авторизованным Катей для ручной проверки клика; системные разрешения
Chrome включены. Браузер полностью не завершали: тест закрытого приложения
означает отсутствие вкладки FamilyTime при работающем Chrome.

Изолированный smoke напоминаний проверил минутный cron, время, завершённые и
архивные записи, права, уникальность, повторные проходы и откат после ошибки.
Go-тесты повторно запущены без cache: шифрование, VAPID, retry, отзыв подписки,
истечение сессии и family isolation. 242 frontend-теста, check и build прошли.

- Дополнение 2026-09-15: владелец подтвердил получение уведомлений на iPhone и desktop,
  баннер macOS, переход в нужную запись на обоих устройствах и архивирование
  оставшейся тестовой записи. Ручные пункты 14.7.3/14.7.4/14.7.6/14.7.7 закрыты;
  описанные выше ожидания подтверждения относятся к предыдущему состоянию.
- SMTP не настроен: отправку писем восстановления пароля нельзя считать рабочей. Обычная смена известного пароля доступна в профиле.
- Внешний мониторинг истечения сертификата/неуспешных backup и регулярная off-server синхронизация ещё не подключены; сейчас есть таймеры и одна проверенная локальная копия.
- Длительная совместная нагрузка приложения и VPN, реальный Safari и разные часовые пояса требуют дальнейшей проверки. Timeline использует локальное время устройства, сводка — время семьи; UX нужно унифицировать. На mobile остаётся проверить высоту карточек с длинными заголовками.

### Stage 14.8: эксплуатационная проверка

Ресурсы, ограниченная совместная нагрузка, восстановление свежего backup и
пробное продление HTTPS проверены. Результаты и ограничения:
[отчёт Stage 14.8](readiness-stage148.md). Временная восстановленная копия удалена;
VPN не перезапускались. Регулярное внешнее резервирование и оповещения о сбоях
по-прежнему требуют отдельной настройки.

### Публикация мобильных исправлений, 2026-09-15

По разрешению владельца опубликован `familytime-20260915T191803`:
мобильные стрелки периода, выравнивание времени, закрытие панелей свайпом;
ограничения выбора профилей на всех устройствах.
Предыдущий релиз: `familytime-20260915T130633`.
Backup: `/var/backups/familytime/familytime-20260915T191843Z.tar.gz`.
Backend binary, hooks и миграции совпадают побайтно с предыдущим релизом.
Сохранены старые immutable assets; Caddy и VPN не перезапускались.
AWG StartedAt `2026-07-10T01:55:28.561280082Z`, restarts=0; wg0 активен.

Публикация выполнена `deploy/update-ui.sh`. Проверены отказ при отличающемся
backend и автоматический откат при неготовом health endpoint. В скрипт добавлено
ожидание запуска PocketBase после backup; следующая публикация прошла.
Неактивные попытки `191442`/`191656` не используются как current.
Инструкция для самостоятельного обновления: [UPDATING](../../deploy/UPDATING.md).
Публичный HTTPS smoke в Chrome прошёл: вход, mobile 320/390 px, обе темы,
геометрия времени, свайп подробностей/создания/сводки, сохранение черновика,
Игорь → Ева → Игорь, desktop-крестик. Новые записи не сохранялись.
Service worker в smoke отключён; обновление установленной PWA на устройстве
этим прогоном не подтверждается. Перед свайпом сводки тест ждёт загрузки событий,
иначе изменение высоты панели делает координаты жеста устаревшими.
